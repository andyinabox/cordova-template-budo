{
  "name": "prototype",
  "version": "0.0.1",
  "description": "Prototype",
  "main": "www/index.js",
  "scripts": {
    "start": "cd app; budo index.js -H localhost --live --open --css styles.css",
    "build": "cd app; budo-demo index.js -o ../www -c styles.css -m",
    "clean": "rm -rf www/*"
  },
  "dependencies": {},
  "devDependencies": {
    "budo": "^9.2.1",
    "budo-demo": "^0.1.3"
  }
}
